/*
Create a simple Javascript function code for addition, subtraction, and multiplication of 2numbers and
write the corresponding Jest based tests for it
* */
const mathOperations = {
  add: (a, b) => a + b,
  sub: (a, b) => a - b,
  mul: (a, b) => a * b,
};

module.exports = mathOperations;
